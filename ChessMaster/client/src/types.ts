// src/types.ts

export interface ZohoUserProfile {
  email: string;
  username: string;
  elo_rating: number;
  total_wins: number;
  total_losses: number;
  total_draws: number;
  total_matches_played: number;
  zoho_record_id: string;
}
